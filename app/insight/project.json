{
  "name": "@intrig-core/insight",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "app/insight/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project @intrig-core/insight --web",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/app/insight"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "@intrig-core/insight:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "@intrig-core/insight:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "@intrig-core/insight:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "@intrig-core/insight:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "@intrig-core/insight:build:development"
        },
        "production": {
          "buildTarget": "@intrig-core/insight:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../coverage/app/insight"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    }
  }
}
