# plugin-next

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build plugin-next` to build the library.

## Running unit tests

Run `nx test plugin-next` to execute the unit tests via [Jest](https://jestjs.io).
